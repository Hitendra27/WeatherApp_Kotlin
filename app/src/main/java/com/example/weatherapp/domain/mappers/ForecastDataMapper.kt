package com.example.weatherapp.domain.mappers

class ForecastDataMapper {

   // fun convertFromDataModel(forecast: ResponseClasses.ForecastResult): ForecastList =
       // ForecastList(forecast.city.name, forecast.city.country)


    // private fun convertForecastListToDomain(list: List<Forecast>)
   // : List<ModelForecast> {
     //   return dt = Calendar.getInstance().timeInMillis +
       //         TimeUnit.DAYS.toMillis(i.toLong())
     //   convertForecastListToDomain(forecast.copy(dt = dt))
   // }
}